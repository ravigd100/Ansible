--- 
  - name: "checking"
    hosts: Test
    remote_user: ravi
    tasks:
    
    - name: "Just small command"
      shell: "cd /home ; ls -lrt ; whoami"
      register: output
      
    - debug: var=output.stdout_lines
